{
  "name": "dual-core-ad-assistant",
  "version": "2.0.0",
  "description": "双核广告助手 SaaS - Cloudflare Pages + Worker",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "pages:dev": "wrangler pages dev frontend",
    "pages:deploy": "wrangler pages deploy frontend"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
